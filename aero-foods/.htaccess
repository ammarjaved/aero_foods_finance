<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /aero-foods/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /aero_foods_finance/index.html [L]
</IfModule>
